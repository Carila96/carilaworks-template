# CARILA WORKS 共通制作ルール

このファイルは、このテンプレートから作成されるすべての作品で Codex が従う共通ルールです。

## 作業を始める前に

- 既存の `README`、仕様書、設計資料、タスク資料、設定ファイル、コードを確認し、作品の目的、現状、制約を把握してから作業する。
- リポジトリ内に、より対象範囲の狭い `AGENTS.md` がある場合は、その指示も確認して従う。
- 要件間の矛盾、判断に必要な情報の不足、または未確定の仕様を見つけた場合は、勝手に確定せずユーザーへ確認する。

## 仕様と変更

- 既存仕様を独断で変更しない。依頼の実現に仕様変更が必要な場合は、影響範囲と選択肢を説明し、ユーザーの承認を得る。
- 依頼された範囲に必要な、最小限で追跡しやすい変更を行う。
- 実装後は、関連するテスト、ビルド、静的解析または妥当性確認を実行し、その結果を報告する。
- 実装とドキュメントの内容を一致させる。作品の構成や運用方法を変えた場合は、該当する資料も更新する。

## プロジェクトメタデータ

- ルートの `carila-project.json` を作品の実態に合わせて維持・更新する。
- テンプレートから作品を初めて制作するときは、`carila-project.json` と `README.md` に残るテンプレートの仮値を、作品固有の名称、識別子、説明などへ変更する。
- `carila-project.json` の `schemaVersion` は、明示された移行作業なしに変更しない。
- 不明なメタデータを推測で埋めない。未確定であることが分かる値を維持するか、ユーザーへ確認する。

## セキュリティと外部操作

- Secret、API キー、アクセストークン、パスワード、秘密鍵、個人情報などの機密情報を Git に保存しない。
- 機密値が必要な場合は、Git 管理外の環境変数または承認された Secret 管理機能を使用し、リポジトリには安全な例示値だけを置く。
- PUBLIC 化、外部公開、デプロイ、リリース、DNS 変更、サブドメインの作成・変更、外部サービスの本番リソース操作は、ユーザーの明示的な承認なしに実行しない。
- 破壊的操作、課金が発生し得る操作、または外部状態を変更する操作は、対象と影響を事前に説明して明示的な承認を得る。

## Git

- 生成物、ローカル設定、機密情報をコミットしない。
- 変更内容と検証結果を明確に報告する。
